apiVersion: apps/v1
kind: Deployment
metadata:
  name: crud-demo
  namespace: crud-demo
  labels:
    app: crud-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crud-demo
  template:
    metadata:
      labels:
        app: crud-demo
    spec:
      containers:
        - name: crud-demo
          image: python-crud-demo:latest
          imagePullPolicy: Never  # use local Docker image, don't pull from registry
          ports:
            - containerPort: 8000
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
